(function ($) {

// Store our function as a property of Drupal.behaviors.
    Drupal.behaviors.heydesk_messenger = {
        attach: function (context, settings) {
            //$('td.red-background', context).parent('tr').addClass('red-background');

        }
    };
}(jQuery));